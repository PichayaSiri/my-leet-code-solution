import { ListNode } from "@daily";

export function swapPairs(head: ListNode | null): ListNode | null {
  let tail = head;
  let swapNode: ListNode | null = null;
  while (tail) {
		
	}
  return head;
}
